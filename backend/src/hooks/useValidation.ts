import { body } from 'express-validator';export const emailValidation = body('email').isEmail().withMessage('Invalid email format').matches(/^[\w-\.]{1,}+@[\w-]{1,}\.(?:com|com\.br)$/).withMessage('Email must end with .com or .com.br');export const passwordValidation = body('password').isLength({ min: 8 }).withMessage('Password must be at least 8 characters long').matches(/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[!@#$%^&*()_+\-=\[\]{};':"\\|,.<>\/?]).{8,}$/).withMessage('Password must contain at least one uppercase letter, one lowercase letter, one number and one special character');export const registerValidation = [emailValidation, passwordValidation];export const loginValidation = [emailValidation, passwordValidation];